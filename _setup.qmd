```{r}
#| label: setup
#| include: false
#| cache: false

library(knitr)
library(tidyverse)

knitr::opts_chunk$set(
    cache = FALSE,
    echo = TRUE,
    comment = "#>",
    collapse = TRUE
)

options(max.print = 200)
options(scipen = 1000)
```